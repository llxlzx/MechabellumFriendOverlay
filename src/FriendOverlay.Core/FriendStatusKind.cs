namespace FriendOverlay.Core
{
    /// <summary>Coarse status buckets used for color and section mapping.</summary>
    public enum FriendStatusKind
    {
        Offline = 0,
        Idle = 1,
        Waiting = 2,
        Pve = 3,
        Battle = 4,
    }
}
