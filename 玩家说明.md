# 好友列表叠加面板

用独立面板查看好友：分区更清楚，搜索筛选更快。加入、观战、私聊、邀请、回关等操作与游戏内一致。

**版本：0.3.35**

## 安装

1. 用「钢铁指挥官 Mod 管理器」在社区目录安装本扩展
2. 确认已启用，然后重启游戏

也可把 `FriendOverlay.dll` 放到游戏的 `Mods` 文件夹（该文件夹里只保留一份即可）。

## 用法

打开好友后默认显示本面板。

| 按键 / 操作 | 作用 |
|-------------|------|
| `F8` | 本面板 ↔ 游戏自带好友窗 |
| 标题页签 | 关注 / 关注我的人 |
| `Ctrl+F` | 搜索 |
| `R` | 刷新 |
| 拖标题栏 / 边框或四角 | 移动 / 缩放 |
| 悬停一行 | 加入 · 观战 · 私聊 · 邀请 |
| `⋯` 或右键 | 置顶等更多操作 |

列表按可加入、对战中、离线分区，支持筛选与排序。

## 说明

- **关注列表**：打开后会自动加载完整人数，状态从「同步中」变为「已同步」即完成。
- **关注我的人**：展示当前能拿到的名单；人数显示为「已加载 N」。可回关。
- **邀请**：已在房间就直接邀请；不在房间时会弹出战斗类型选择（1对1、2对2、生存模式、组队匹配、4人混战、时空裂隙 1V1/2V2），选好后自动开房并邀请，按钮期间显示「邀请中」。对方不在线时按钮不可用。
- **置顶**：仅保存在本机，最多 20 人，重启后仍在。
- **字体：** 0.3.35 起默认随包内嵌 **Noto Sans SC（思源黑体）**；失败时回退雅黑动态字体。若要贴近游戏原生字体，可在 MelonPreferences → Friend Overlay → UseGameFont 开启。
- **窗口：** 0.3.34 默认更矮，避免挡住底部邀请提示与自身资料；过大旧窗口会在升级时自动收一档安全高度。0.3.35 起可拖**四边/四角**缩放（标题栏仍可拖动移动）。
- **进对局：** 0.3.34 起进入加载/对局会自动关闭本面板（回大厅后需再点好友按钮打开）。
- **头像：** 与游戏内选择一致；加载失败时显示名字首字母。0.3.24+ 修复头像框错位/拼贴；0.3.25 修复因 bake 预算耗尽被误判为永久失败的头像框；0.3.32/0.3.33 修复官方头像 bake 临时物体泄漏到大厅中间（请用管理器更新到目录最新版）。
- **性能：** 关闭好友面板会释放头像缓存（下次打开需重新加载）。官方动态头像/头像框默认只 bake 静态第 1 帧；可在 MelonPreferences → Friend Overlay → AnimatedOfficialAvatars 开启多帧动画。

## 小提示

- 面板打不开：确认已启用并重启，再按 `F8` 切换。
- 想用回游戏自带界面：在管理器里停用本扩展后重启即可。
